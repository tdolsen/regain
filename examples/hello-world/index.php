<?php

// Uncomment lines below if you can't change the include_path elsewhere (make sure the "regain" library folder is on include path)
//set_include_path('../libs' . PATH_SEPARATOR . get_include_path());

// Depending on setup, but this should be all to get going
require_once 'regain/bootstrap.php';
